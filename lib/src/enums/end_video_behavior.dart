enum EndVideoBehavior {
  /// The video stays on the last frame
  wsDefault,

  /// The video shows the thumbnail and controls if they are set to be visible on load.
  reset,

  /// The video plays again from the beginning
  loop
}
